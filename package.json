{
  "name": "guise",
  "description": "implies masking or theming the underlying tool",
  "version": "0.0.1",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=25.x"
  },
  "packageManager": "pnpm@10.20.0",
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "format": "turbo run format --continue -- --cache --cache-location ./node_modules/.cache/format.log",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.6",
    "turbo": "^2.6.1",
    "typescript": "^5.9.3"
  }
}
